import random

# al random picking
last_number = None

def random_less_repeats():
    global last_number
    new_number = random.randint(0, 2)
    if new_number == last_number:
        new_number = random.randint(0, 2)
    last_number = new_number
    return new_number

if random_less_repeats == 0 and 
if random_less_repeats == 0 and 
if random_less_repeats == 0 and 

if random_less_repeats == 1 and 
if random_less_repeats == 1 and 
if random_less_repeats == 1 and 

if random_less_repeats == 2 and 
if random_less_repeats == 2 and 
if random_less_repeats == 2 and 

